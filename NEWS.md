# bkcde news

## 1.47

- Fixed infinite-bound handling in `bkcde()` and `bkcde.optim()` for proper
  estimation and optimization.
- Improved package check hygiene by tightening tarball contents, modernizing
  metadata, and reducing checked examples to serial-friendly workloads.
- Added lightweight `testthat` regression coverage for core estimator paths.
